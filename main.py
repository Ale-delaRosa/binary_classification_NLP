# Import the training and evaluation function from the neural_networks_keras module
from src.binary import train_imdb_model

# Check if the script is running directly
if __name__ == "__main__":  
   train_imdb_model()  # Call the function to train and evaluate the neural network
